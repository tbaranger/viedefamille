[project]
name = "viedefamille"
version = "0.1.0"
description = "A simple family organisation tool"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.3",
    "ruff>=0.15.12",
]
